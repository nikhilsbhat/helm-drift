name: drift
flags:
  - log-level
  - no-color
  - set
  - set-file
  - set-string
  - skip-tests
  - values
commands:
  - name: run
    flags:
      - from-release
      - regex
      - skip-cleaning
      - skip-validation
      - summary
      - temp-path
  - name: version