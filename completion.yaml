name: drift
flags:
  - log-level
  - no-color
  - set
  - set-file
  - set-string
  - skip-tests
  - values
commands:
  - name: run
    flags:
      - custom-diff
      - disable-error-on-drift
      - from-release
      - json
      - regex
      - report
      - skip-cleaning
      - skip-validation
      - summary
      - temp-path
      - yaml
  - name: all
    flags:
      - custom-diff
      - disable-error-on-drift
      - is-default-namespace
      - json
      - regex
      - report
      - skip-cleaning
      - skip-validation
      - summary
      - temp-path
      - yaml
  - name: version