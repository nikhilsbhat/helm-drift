name: drift
flags:
  - log-level
  - no-color
  - set
  - set-file
  - set-string
  - skip-tests
  - values
commands:
  - name: run
    flags:
      - disable-error-on-drift
      - from-release
      - json
      - regex
      - report
      - skip-cleaning
      - skip-validation
      - summary
      - temp-path
      - yaml
  - name: version