name: drift
flags:
  - log-level
  - set
  - set-file
  - set-string
  - skip-tests
  - values
commands:
  - name: run
    flags:
      - from-release
      - regex
      - skip-cleaning
      - skip-validation
      - temp-path
  - name: version